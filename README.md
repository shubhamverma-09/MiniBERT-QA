# MiniBERT-QA
Implemented a MiniBERT-style Transformer for Extractive Question Answering from scratch using TensorFlow/Keras. Features BERT embeddings, multi-head self-attention, transformer encoders, custom SQuAD preprocessing, mixed precision training, warmup scheduling, gradient clipping, and EM/F1 evaluation without relying on pretrained BERT models.
